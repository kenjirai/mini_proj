import React from "react";

class Deployment extends React.Component {
  contructor(props) {
    super(props);
    this.state={};
  }

  return() {
    render(
      <h2>Deployment Information</h2>
    );
  }
}
