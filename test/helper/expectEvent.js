
//check the event name
// check the event value
/*
process.argv.forEach(function (val) {
	console.log('inside', val)
  if(val == 'web3') {
  	console.log('inside capter')
  	const { web3 } = require('./debugUtils');
});



function loopObj(obj) {
	Object.entries(obj).forEach(([key, value]) => {
		if(key == 'event') {
			console.log('eventValue: ', value);
		}
	})
}

function expectEvent(obj, eventName, eventValue, fromBlock, toBlock='latest') {
	myContract.getPastEvents(eventName, {
	    filter: {eventName: [20,23]}, // Using an array means OR: e.g. 20 or 23
	    fromBlock: fromBlock,
	    toBlock: toBlock
	}).then((data) => {
		loopObj(data[0])
	})
}

*/





